# Server Configuration
NODE_ENV=development
PORT=3000
HOST=localhost

# Database Configuration
MONGODB_URI=mongodb://localhost:27017/cal_ai
MONGODB_TEST_URI=mongodb://localhost:27017/cal_ai_test

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# CORS Configuration
CORS_ORIGIN=http://localhost:3001

# Logging
LOG_LEVEL=info

# External Services (add as needed)
# EMAIL_API_KEY=
# CLOUDINARY_URL=
# STRIPE_SECRET_KEY=

# Cafe Bazaar Configuration
# Get token from: https://pardakht.cafebazaar.ir/ → Your App → API پیشخان بازار → دریافت توکن جدید
# Token is sent as header: CAFEBAZAAR-PISHKHAN-API-SECRET
CAFEBAZAAR_ACCESS_TOKEN=your-cafebazaar-pishkhan-api-secret-token-here
CAFEBAZAAR_PACKAGE_NAME=com.yourapp.package

# Zarinpal Payment Gateway Configuration
# Get merchant ID from: https://panel.zarinpal.com → درگاه های پرداخت → کد پذیرنده
# Set ZARINPAL_SANDBOX=true for testing with sandbox environment
ZARINPAL_MERCHANT_ID=your-zarinpal-merchant-id-here
ZARINPAL_SANDBOX=false

# App URLs for payment callbacks
APP_URL=https://loqmeapp.ir
WEBAPP_URL=https://loqmeapp.ir/app

# Telegram Notification Configuration
# To get a bot token: Search for @BotFather on Telegram, create a new bot
# To get a channel ID: Add bot to channel, forward a message from channel to @userinfobot or https://api.telegram.org/bot<TOKEN>/getUpdates
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
TELEGRAM_CHANNEL_ID=your-telegram-channel-id

# RevenueCat Configuration (for Global / App Store / Play Store payments)
# Get API key from: https://app.revenuecat.com → Your Project → API Keys → Secret API keys
REVENUECAT_API_KEY=your-revenuecat-secret-api-key-here
